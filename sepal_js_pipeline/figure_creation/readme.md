# Figure Creation

## Earth Engine Authentication

Prior to running `main.py`, ensure you run the below line to authenticate your computer with the Google Cloud Account that you have registered with Google Earth Engine.

```{bash}
gcloud auth application-default login
```
